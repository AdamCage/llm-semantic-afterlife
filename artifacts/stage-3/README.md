# Stage 3 artifacts

Publication-grade outputs for Stage 3. Each figure or table ships with
tidy source data and a `.meta.json` caption.

This directory is populated by the S3.0 surface diagnostics and the
S3.1 dynamics pass. It is empty at plan time on purpose.
